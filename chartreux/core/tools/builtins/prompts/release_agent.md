Release a retained background subagent and close its runtime. Supply `agent_id`. If the agent is still running, its current task is interrupted and any partial work is discarded. After release, the agent handle is invalid and retained run results are discarded. Releasing an evicted agent is valid and removes its retained tombstone and results. Use release when an engagement concludes or an agent's work is no longer needed.
